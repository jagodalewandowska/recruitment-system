spring.datasource.url=jdbc:postgresql://localhost:5432/rekrutacja
spring.datasource.username=postgres
spring.datasource.password=politechnika
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate settings (if using JPA)
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto = update

# Logging JPA Queries
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

server.port=8081

# Spring Security
# HTTP authentication credentials
spring.security.user.name=admin
spring.security.user.password=admin
# tokeny
jwt.secret-key=d0a9c24c3f1e4d249109feaa72dce0a4
jwt.token-validity-in-min=60
# debug
logging.level.org.springframework.web= DEBUG
